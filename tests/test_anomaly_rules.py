"""Unit tests for the anomaly detection rules in shared/nb_data_quality.py."""

# TODO (Lab 5): one test per ANOMALY_RULES entry, boundary values included
# (battery == 10, gap_seconds == 300, ingest_delay_seconds == 30).
